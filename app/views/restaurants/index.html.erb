<h1>Restaurants#index</h1>

<ul>
<% @restaurants.each do |restaurant| %>
<li><%= link_to restaurant.name, restaurant_path(restaurant) %></li>
<% end %>
</ul>

<%= link_to "Create New", new_restaurant_path(@restaurant), class: "btn btn-primary"%>
